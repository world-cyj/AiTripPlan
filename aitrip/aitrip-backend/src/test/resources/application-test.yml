spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aitrip?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: aitrip123
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: aitrip-test-group
      auto-offset-reset: earliest

logging:
  level:
    com.aitrip: DEBUG
